import React from "react";

import PublishForm from "../components/organisms/PublishForm";
import Small from "../components/templates/Small";

const Publish: React.FC = () => {
  return (
    <Small>
      <PublishForm />
    </Small>
  );
};

export default Publish;
